# build.sh clones OpenWrt here
/openwrt/
# built images ship as GitHub releases, not in-repo
images/*.bin
images/*.itb

# python bytecode
__pycache__/
*.pyc

# bench session artifacts (see docs/BENCH.md, tools/bench/)
dir842-uart.log
daemon.log
spammer.out
uart-cmd.pending
esc-spam.flag
# local flash-back images; large binaries, ship as releases
known-good-images/
# old ggbruno 4.14 checkout kept as reference during the 6.18 port
/openwrt-4.14/
